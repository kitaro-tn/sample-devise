<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div class="row"
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% user.errors.full_messages.each do |message| %>
          <li class="red-text text-lighten-3"><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>

  <% end %>

  <div class="actions">
    <%= f.submit class: "btn amber-text text-lighten-5 amber darken-2" %>
  </div>
<% end %>
